# [Module Name] — Functional Reference

> Generated by /evyasys:CreateFunctionalDocs. Update after significant business logic changes.
> **RAG note:** Each section below is self-contained and can be retrieved independently to answer user questions.

---

## Module Overview

[1–3 sentences. What business problem does this module solve? Who uses it? What is its primary output or outcome? Write for a non-technical reader.]

---

## Entities

What data does this module own and manage?

| Entity | What it represents | Key attributes |
|---|---|---|
| [EntityName] | [Plain-language description] | [field1, field2, status, created_at] |

**Status values** (if applicable):

| Status | Meaning |
|---|---|
| [Draft] | [What it means in business terms] |
| [Active] | [...] |
| [Archived] | [...] |

---

## Access & Permissions

Who can access this module and what can they do?

| Role | View | Create | Edit | Delete | Special restrictions |
|---|---|---|---|---|---|
| [Admin] | ✅ | ✅ | ✅ | ✅ | None |
| [Manager] | ✅ | ✅ | ✅ | ❌ | Own department only |
| [Staff] | ✅ | ❌ | ❌ | ❌ | Read-only |
| [Guest] | ❌ | ❌ | ❌ | ❌ | No access |

**Access rules:**
- [Plain-language rule — e.g. "Users can only view records belonging to their own account. Viewing another account's records is blocked even if they know the URL."]
- [Ownership rule — e.g. "A manager can edit any record in their department, but not records from other departments."]
- [State-based rule — e.g. "Once a record is Approved, only Admins can make further changes."]

---

## Validations

What rules must be satisfied before data is accepted or an action can proceed?

### [Entity or Action name — e.g. Creating a Record]

| Field | Rule | Error shown to user |
|---|---|---|
| Name | Required. Maximum 100 characters. | "Name is required" / "Name cannot exceed 100 characters" |
| Email | Valid email format. Must be unique across the system. | "Please enter a valid email address" / "This email is already in use" |
| [field] | [rule] | [message] |

**Business constraints** (rules that are not tied to a single field):
- [e.g. "A user cannot be deactivated if they are the only Admin on the account. The system shows: 'At least one Admin must remain active.'"]
- [e.g. "An order cannot be cancelled after it has been dispatched. The cancellation button is hidden once the order status is Shipped."]
- [e.g. "A budget cannot be set below the sum of approved expenses already recorded against it."]

### [Another action or entity, if different rules apply]
[Repeat table format above]

---

## Actions

What can users do in this module?

| Action | Who can | Preconditions | What the system does | Outcome |
|---|---|---|---|---|
| Create [entity] | [Role] | Account must be active | Validates fields, saves record, sends confirmation email | New record in Draft status; user sees success message |
| Edit [entity] | [Role] | Record must not be Approved | Validates changes, updates record, logs change with timestamp | Record updated; audit trail entry created |
| Delete [entity] | [Admin] | No active children records | Soft-deletes the record | Record removed from active lists; recoverable for 30 days |
| [Business action] | [Role] | [...] | [...] | [...] |
| Export to PDF | [Role] | At least one record selected | Generates PDF with header, table, footer | PDF downloaded to user's browser |
| Approve [entity] | [Approver role] | Record in Pending Approval status | Marks record Approved, notifies requester, locks for further edits | Record status changes to Approved; requester notified by email |

---

## Business Logic

The rules that govern how this module behaves. Each rule describes a decision the system makes.

### [Rule area — e.g. Status Transitions]

[2–4 sentences. Describe the business rule or decision in plain language.]

**Scenarios:**
- *When X happens:* [What the system does and why]
- *If condition A and condition B:* [What the system decides]
- *If the user has role X and tries to do Y while record is in state Z:* [What the system does]

### [Rule area — e.g. Automatic Calculations]

[Description of the calculation or derived value.]

**Example:** [Concrete example using realistic values — e.g. "If a subscription is renewed mid-month, the system prorates the charge: 15 remaining days out of 30 = 50% of the monthly rate."]

### [Rule area — e.g. Notifications and Triggers]

[When the system automatically sends notifications, emails, or triggers other actions.]

- [e.g. "When a record moves to Approved status, the requester receives an email with a summary and next steps."]
- [e.g. "If no action is taken on a Pending record for 7 days, the system sends a reminder to the assigned approver."]
- [e.g. "When a budget is exceeded by any approved expense, the Finance Admin is notified immediately."]

---

## Workflows

Multi-step processes this module supports.

### [Workflow name — e.g. Approval Flow]

**What triggers this:** [e.g. "A manager submits a record for approval"]
**Who is involved:** [e.g. "Requester, Department Manager, Finance Admin"]

| Step | Who acts | What they do | System response |
|---|---|---|---|
| 1 | Requester | Submits the record for approval | Status changes to Pending Approval; assigned approver is notified |
| 2 | Approver | Reviews the record | Approves or rejects with a comment |
| 3a | System (on Approve) | — | Status set to Approved; requester notified; record locked |
| 3b | System (on Reject) | — | Status returns to Draft; requester notified with rejection reason |
| 4 | Requester (if rejected) | Edits and resubmits | Cycle repeats from Step 1 |

**Happy path:** Requester submits → Approver approves → Record is Approved and locked.
**Failure paths:**
- Approver rejects → Requester revises and resubmits (can repeat up to [N] times).
- No action in [N] days → System sends escalation reminder; after [M] more days, escalates to next approver.
- Approver account is deactivated mid-flow → Record is reassigned to the department's backup approver.

### [Another workflow if applicable]
[...]

---

## Error Scenarios

What can go wrong and how does the system respond?

| Scenario | What the user sees | What the system does |
|---|---|---|
| Duplicate submission | "A record with this name already exists." | Rejects with validation error; no record created |
| Insufficient permissions | "You don't have access to perform this action." | Returns access denied; access attempt is logged |
| External service unavailable | "Unable to complete this action. Please try again shortly." | Queues operation for retry; operations team alerted if failure persists |
| Session expired mid-action | Redirected to login page | Pending changes are discarded; user must re-enter |
| Required dependency missing | "This action requires [X] to be set up first." | Blocks the action; shows link to set up the dependency |
| Concurrent edit conflict | "Another user modified this record. Please review their changes before saving." | Rejects stale save; shows diff for review |

---

## Integration Points

How does this module connect to the rest of the system or external services?

| Integration | Direction | What is exchanged | When it happens |
|---|---|---|---|
| [Notifications module] | Outbound | Notification event with record ID, recipient, message | When status changes to Approved or Rejected |
| [Audit Log] | Outbound | Change record (user, field, old value, new value, timestamp) | On every Create, Edit, Delete, and status change |
| [External API / Service] | Inbound | [Data received and what triggers the call] | [Trigger condition] |
| [Another module] | Both | [Event or data shared] | [When] |

---

## Glossary

Terms specific to this module that may confuse someone new.

| Term | Definition |
|---|---|
| [Term] | [Plain-language definition, specific to how this module uses the term] |
| [Term] | [...] |
