> Not applicable — this project is a Claude Code plugin (CLI tool). There is no browser-based UI. User-facing output is text rendered in the Claude Code chat interface. UX conventions for this plugin are defined in `.ai/core/output-contract.md` (deterministic primary artifact, clear status, next action, human confirmation) and `.ai/core/human-confirmation.md` (show draft → verify intent → iterate → confirm → execute).
